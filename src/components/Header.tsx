import React from "react";

const Header = () => (
    <div className="header py-2">
        <div className="container">
            <h1 className="has-text-centered">Hangman The Game</h1>
        </div>
    </div>
);

export default Header;